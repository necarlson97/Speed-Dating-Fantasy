using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class ScrollScript : MonoBehaviour {
    // void Update() {
    //     // Because canvas is scaled down, need to scale scroll up
    //     if (Input.GetAxis("WheelScroll") > 0) {
    //         GetComponent<ScrollRect>().verticalNormalizedPosition  += scrollSpeedMouse * 100;
    //     }
    // }
}
